// import { createFFmpeg, fetchFile } from "@ffmpeg/ffmpeg";

// const ffmpeg = createFFmpeg({ log: true });
export default function convertWavToMp3() {
  // await ffmpeg.load();
  // // Write the wav file to ffmpeg's in-memory filesystem
  // ffmpeg.FS("writeFile", "input.wav", await fetchFile(wavBlob));
  // // Run the ffmpeg command to convert
  // await ffmpeg.run("-i", "input.wav", "output.mp3");
  // // Read the result from the in-memory filesystem
  // const mp3Data = ffmpeg.FS("readFile", "output.mp3");
  // return new Blob([mp3Data.buffer], { type: "audio/mp3" });
  return;
}
